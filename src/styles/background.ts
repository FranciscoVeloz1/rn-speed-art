import { StyleSheet } from 'react-native';
import { vars } from './vars';

export const background = StyleSheet.create({
  bg_primary: {
    backgroundColor: vars.colors.primary,
  },

  bg_dark_primary: {
    backgroundColor: vars.colors.dark_primary,
  },

  bg_very_dark_primary: {
    backgroundColor: vars.colors.very_dark_primary,
  },

  bg_info: {
    backgroundColor: vars.colors.info,
  },

  bg_dark_info: {
    backgroundColor: vars.colors.dark_info,
  },

  bg_danger: {
    backgroundColor: vars.colors.danger,
  },

  bg_dark_danger: {
    backgroundColor: vars.colors.dark_danger,
  },

  bg_success: {
    backgroundColor: vars.colors.success,
  },

  bg_secondary: {
    backgroundColor: vars.colors.secondary,
  },

  bg_light_secondary: {
    backgroundColor: vars.colors.light_secondary,
  },

  bg_very_light_secondary: {
    backgroundColor: vars.colors.very_light_secondary,
  },

  bg_dark: {
    backgroundColor: vars.colors.dark,
  },

  bg_white: {
    backgroundColor: vars.colors.white,
  },
});
