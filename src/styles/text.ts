import { StyleSheet } from 'react-native';
import { vars } from './vars';

export const text = StyleSheet.create({
  text_primary: {
    color: vars.colors.primary,
  },

  text_dark_primary: {
    color: vars.colors.dark_primary,
  },

  text_very_dark_primary: {
    color: vars.colors.very_dark_primary,
  },

  text_info: {
    color: vars.colors.info,
  },

  text_dark_info: {
    color: vars.colors.dark_info,
  },

  text_danger: {
    color: vars.colors.danger,
  },

  text_dark_danger: {
    color: vars.colors.dark_danger,
  },

  text_success: {
    color: vars.colors.success,
  },

  text_secondary: {
    color: vars.colors.secondary,
  },

  text_light_secondary: {
    color: vars.colors.light_secondary,
  },

  text_very_light_secondary: {
    color: vars.colors.very_light_secondary,
  },

  text_dark: {
    color: vars.colors.dark,
  },

  text_white: {
    color: vars.colors.white,
  },

  // Sizes
  text_xs: {
    fontSize: vars.xs,
  },

  text_sm: {
    fontSize: vars.sm,
  },

  text_md: {
    fontSize: vars.md,
  },

  text_lg: {
    fontSize: vars.lg,
  },

  text_xl: {
    fontSize: vars.xl,
  },

  text_xl2: {
    fontSize: vars.xl2,
  },

  text_xl3: {
    fontSize: vars.xl3,
  },

  text_xl4: {
    fontSize: vars.xl4,
  },

  //Positions
  text_center: {
    textAlign: 'center',
  },

  text_auto: {
    textAlign: "auto",
  },

  text_justify: {
    textAlign: "justify",
  },

  text_left: {
    textAlign: "left",
  },

  text_right: {
    textAlign: 'right',
  },
});
